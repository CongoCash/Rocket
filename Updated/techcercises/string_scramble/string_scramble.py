# Using the Python language, have the function string_scramble(str1,str2) take both parameters being passed
# and return boolean True if a portion of str1 characters can be rearranged to match str2,
# otherwise return boolean False

# For example: if str1 is "rkqodlw" and str2 is "world" the output should return true.
# Punctuation and symbols will not be used.

# Input = "cdore" & str2= "coder" Output = True
# Input = "h3llko" & str2 = "hello" Output = False

def string_scramble(str1, str2):
	pass